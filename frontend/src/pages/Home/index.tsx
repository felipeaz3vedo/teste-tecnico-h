import { Header } from '../../components/Header';
import { Transactions } from '../../components/Transactions';

export const Home = () => {
  return (
    <div>
      <Header />
      <Transactions />
    </div>
  );
};
